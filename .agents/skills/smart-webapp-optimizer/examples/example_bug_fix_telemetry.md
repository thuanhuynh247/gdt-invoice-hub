# Ví dụ 2: Khắc phục lỗi tính toán số liệu & Đồng bộ Harness

**Yêu cầu của người dùng**: "Sửa lỗi tính sai tỷ lệ phế chải bông PX2"

## Quy trình thực hiện:

### Bước 1: Phát hiện Bất thường Dữ liệu (Smart Data Analysis)
1. AI chạy `smart_data_reader.py eda` và DuckDB query trên `carding_waste` table.
2. AI phát hiện tỷ lệ phế chải tính ra `12.5%` (vượt ngưỡng cho phép `4.5%`) do lỗi chia nhầm tổng sản lượng thay vì đầu vào.

### Bước 2: Thiết lập Vòng lặp Kiểm thử TDD (Ask Matt TDD)
1. AI chạy `pytest tests/test_carding_analytics.py -k test_waste_ratio` -> Kiểm thử báo lỗi RED (Assertion Error).
2. AI lập kế hoạch sửa lỗi công thức trong `app/db/views.py`.

### Bước 3: Khắc phục & Chạy Kiểm thử Phân cấp
1. AI điều chỉnh câu lệnh SQL SQL view trong `app/db/views.py`.
2. AI chạy lại `pytest tests/test_carding_analytics.py` -> GREEN (Pass).
3. AI chạy `npm --prefix web run build` để đảm bảo frontend hiển thị đúng số liệu mới không đứt gãy.

### Bước 4: Đồng bộ Telemetry & Nghiệm thu
1. AI cập nhật file `walkthrough.md` giải thích chi tiết nguyên nhân sai công thức và số liệu thực tế đã sửa.
2. AI chạy `python scripts/harness_win.py trace` để đẩy telemetry kiểm thử lên Harness Trace Visualizer.

<!-- Generated by Skill Creator Ultra v1.0 -->
