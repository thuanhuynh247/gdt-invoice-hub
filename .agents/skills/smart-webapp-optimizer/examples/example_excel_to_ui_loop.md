# Ví dụ 1: Tối ưu trang Báo cáo Công đoạn từ file Excel nguồn

**Yêu cầu của người dùng**: "Tối ưu lại giao diện hiển thị máy ống theo đúng file báo cáo Excel ngày 21/08"

## Quy trình thực hiện:

### Bước 1: Bóc tách Dữ liệu (Smart Data Grounding)
1. AI chạy `smart_data_reader.py info` và `preview` trên file `Mayong-Sanluong-px1.xlsx`.
2. AI phát hiện 4 sheet dữ liệu: `Sản lượng`, `Lỗi cắt`, `Classimat`, `Lũy kế ca bao`.
3. AI sinh file ghi chú sidecar `.data.Mayong-Sanluong-px1.xlsx.md` lưu lại thông tin schema.

### Bước 2: Định hình Kiến trúc & Lập Ticket (Ask Matt Flow)
1. AI sử dụng `grill-with-docs` rà soát `AGENTS.md` để đảm bảo không hiển thị số sai khi thiếu mẫu số (NP.2).
2. AI lập `implementation_plan.md` phác thảo 4 tab giao diện trong `WindingView.jsx`.
3. AI tạo ticket tracer trong `.scratch/winding_ui/issues/T001.md`.

### Bước 3: Thực thi Tối ưu UI/UX Wise Fintech
1. AI cập nhật `WindingView.jsx` bằng công cụ `replace_file_content`.
2. Bổ sung các thẻ KPI bento grid glassmorphic, widget ma trận lỗi Classimat 4x4 và biểu đồ ECharts sản lượng PX1 vs PX2.

### Bước 4: Kiểm thử Phân cấp & Sửa lỗi
1. AI chạy `pytest tests/test_winding_stage.py` -> Thành công (Pass).
2. AI chạy `scripts\validate.bat` -> 100% test suite đạt.

### Bước 5: Đồng bộ Harness & Telemetry
1. AI ghi nhận kết quả vào `walkthrough.md` và đánh dấu `[x]` hoàn thành trong `task.md`.
2. AI chạy `python scripts/harness_win.py unified-gate` để ghi log telemetry vào `harness.db`.

<!-- Generated by Skill Creator Ultra v1.0 -->
